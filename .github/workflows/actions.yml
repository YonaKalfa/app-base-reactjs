name : Build and test

on :
  push :

jobs :
    Build-and-test :
    runs-on : ubuntu

    steps :
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                node-version: 20 
                cache: npm